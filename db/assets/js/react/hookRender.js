import React from "react";
import ReactDOM from "react-dom";

import Hook from "./module/Hook";

ReactDOM.render(
    <React.StrictMode>
        <Hook />
    </React.StrictMode>,
    document.getElementById('firstHook')
);